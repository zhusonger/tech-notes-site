# 锁定公网源，覆盖机器级 npm 配置。
#
# 背景：开发机上的 ~/.npmrc 常指向自建镜像。一旦在它下面生成 package-lock.json，
# 锁文件里的 resolved 就会写死那个（通常只在办公网可达的）域名 —— 换一台机器、
# 或在容器里构建时 npm ci 会集体超时，而报错只显示网络失败，
# 很难联想到根因在锁文件里。
#
# 项目级 .npmrc 优先级高于用户级，保证无论在哪台机器上装依赖，锁文件里的
# resolved 始终是公网规范地址，构建不依赖任何内网服务。
registry=https://registry.npmjs.org/
